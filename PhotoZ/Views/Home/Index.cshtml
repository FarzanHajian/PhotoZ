@using MongoDB.Driver.GridFS;

@{
    if (ViewBag.Photos != null)
    {
        foreach (GridFSFileInfo fileInfo in ViewBag.Photos)
        {
            <img class="thumbnail col-md-4" src="@Url.Action("GetPhoto", new { id = fileInfo.Id })" />
        }
    }
}